  <!-- Navbar -->
   
  <nav class="bg-white shadow-lg relative sticky top-0 w-full z-10  ">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex justify-between h-16">
        <div class="flex-shrink-0 flex items-center">
          <!-- Brand Name -->
          <a href="#" class="text-3xl font-bold "style="color: #fe424d;">Wanderlust</a>
        </div>
        <div class="hidden md:flex items-center space-x-10">
          <!-- Navbar Links -->
          <a href="/listings" class="text-gray-600 hover:font-bold">Home</a>
          <a href="/listings/new" class="text-gray-600 hover:font-bold">Publish a Hotel</a>
         
          <% if (!currentUser) { %>
          <a href="/login" class="bg-pink-600 text-white font-bold px-4 py-2 rounded-md hover:bg-pink-700">Login Now</a>
          <% } else { %>
          <a href="/logout" class="bg-green-600 text-white font-bold px-4 py-2 rounded-md hover:bg-green-700">Logout</a>
          <% } %>
        </div>
        <!-- Mobile Menu Button -->
        <div class="md:hidden flex items-center">
          <button id="mobile-menu-btn" class="text-gray-600 focus:outline-none">
            <svg class="w-6 h-6" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24" stroke="currentColor">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16m-7 6h7" />
            </svg>
          </button>
        </div>
      </div>
    </div>

    <!-- Mobile Menu -->
    <div id="mobile-menu" class="md:hidden hidden bg-white px-2 pt-2 pb-3 space-y-4 transition-all duration-100 ease-in-out">
      <a href="/listings" class="block text-gray-600 hover:text-blue-600">Home</a>
      <a href="/listings/new" class="block text-gray-600 hover:text-blue-600">Create Listing</a>
<% if (!currentUser) { %>
      <a href="/login" class="block bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-blue-700 inline-block">Login Now</a>
 <% } else { %>
     <a href="/login" class="block bg-blue-600 text-white px-4 py-2 rounded-md hover:bg-green-700 inline-block">Logout</a>
<% } %>
    </div>
  </nav>
